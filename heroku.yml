setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
run:
  web: npm run check && npm run postinstall && prisma migrate up --experimental && npm run dev